# Replit configuration for GAINS Food Vision API

run = "bash scripts/setup_and_validate.sh && uvicorn main:app --host 0.0.0.0 --port $PORT"

entrypoint = "main.py"

[nix]
channel = "stable-23_11"

[deployment]
run = ["sh", "-c", "uvicorn main:app --host 0.0.0.0 --port $PORT"]

[[ports]]
localPort = 8000
externalPort = 80
